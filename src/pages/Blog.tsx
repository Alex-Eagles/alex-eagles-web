import ComingSoon from "@/pages/ComingSoon";

/** Blog page — placeholder until the real design lands. */
export default function Blog() {
  return (
    <ComingSoon
      title="Weekly Updates"
      blurb="Our build log — tracking progress across every subteam, week by week. Filtering and search are on the way as this page comes together."
    />
  );
}
