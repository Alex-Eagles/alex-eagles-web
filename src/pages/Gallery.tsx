import ComingSoon from "@/pages/ComingSoon";

/** Gallery page — placeholder until the real design lands. */
export default function Gallery() {
  return (
    <ComingSoon
      title="Gallery"
      blurb="Behind the scenes of Team Alex Eagles — build process, test flights, and events. A media gallery will live here shortly."
    />
  );
}
